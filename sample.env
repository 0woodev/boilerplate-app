# ============================================================
# Project
# ============================================================
PROJECT_NAME="my-app"
DOMAIN="0woodev.com"

# ============================================================
# GitHub
# GITHUB_OWNER_TYPE: "user" | "org"
# GITHUB_VISIBILITY: "public" | "private"
# GITHUB_TOKEN: GitHub API 호출(레포 생성)에만 사용 (repo 권한 필요)
#   git clone/push 는 로컬 git credential (keychain, SSH 등) 사용
#   org 사용 시 SSO 설정된 경우 token 에서 "Configure SSO → Authorize" 필요
# ============================================================
GITHUB_OWNER="0woodev"
GITHUB_OWNER_TYPE="user"
GITHUB_VISIBILITY="public"
GITHUB_TOKEN=""

# ============================================================
# Boilerplate 소스 레포
# ============================================================
BOILERPLATE_OWNER="0woodev"
BOILERPLATE_FE_REPO="boilerplate-fe"
BOILERPLATE_BE_REPO="boilerplate-be"

# ============================================================
# AWS
# ============================================================
AWS_REGION="ap-northeast-2"
AWS_ACCOUNT_ID=""

# ============================================================
# Derived (자동 계산 - 수정 불필요)
# ============================================================
FE_REPO_NAME="${PROJECT_NAME}-fe"
BE_REPO_NAME="${PROJECT_NAME}-be"
TF_STATE_BUCKET="${PROJECT_NAME}-tf-state"
FE_DOMAIN="https://${PROJECT_NAME}.${DOMAIN}"
BE_DOMAIN="${PROJECT_NAME}-api.${DOMAIN}"
